# JWT Configuration
JWT_SECRET=very-secret-jwt-key-for-development-purposes-only
JWT_REFRESH_SECRET=very-secret-refresh-jwt-key-for-development-purposes-only
JWT_ACCESS_TOKEN_EXPIRY=15m
JWT_REFRESH_TOKEN_EXPIRY=7d

# Replit OIDC Configuration (for compatibility)
ISSUER_URL=https://replit.com/oidc
REPL_ID=camp-management-app

# Session Configuration (no longer used with JWT but kept for compatibility)
SESSION_SECRET=session-secret-for-backward-compatibility

# Database Configuration
DATABASE_URL=postgresql://postgres:password@localhost:5432/camp_management